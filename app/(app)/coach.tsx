// Placeholder route for the center nav orb. The tab's button is overridden to
// open the voice overlay (see _layout.tsx), so this screen never actually shows.
export default function CoachPlaceholder() {
  return null;
}
