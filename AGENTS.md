# Agent context

The full project context, architecture, data model, decisions, run instructions,
and session history live in **[CLAUDE.md](./CLAUDE.md)** — read that first.

(Quick orientation: **Beckon** — codename "showup" — is a voice-first AI goal coach.
Expo SDK 52 + React Native + Supabase + Gemini Live native-audio. Brand-purple UI,
velocity-% not streaks. Secrets live in gitignored `.env` / `.env.supabase`, never here.)
